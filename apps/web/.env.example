# API Configuration
VITE_API_BASE_URL=http://localhost:3000

# Feature Flags
# Set to 'true' to enable authentication, 'false' to disable it
# When false: all routes redirect to /map, login and app routes are blocked
# When true: normal authentication flow (login required for /app/* routes)
VITE_ENABLE_AUTH=true
